# 開発ルール

## 基本方針

- このプロジェクトは完全ローカルアプリです。Bancho、osu!公式サーバー、osu! APIへ接続する機能を追加しないでください。
- `.osz` / `.osu` のImportとGameplayを分離してください。ImporterがファイルをLibrary用のローカル構造へ取り込み、GameplayはBeatmap/Library modelを使います。Gameplayからarchiveを直接読み込まないでください。
- RendererとGameplay logicを分離してください。Rendererは`GameplayState`を描画し、Scoreや判定状態を変更しません。
- ゲーム時刻は共通の`GameClock`を通してください。Gameplay内で個別に実時間を読む処理を増やさないでください。
- Ruleset固有処理はRuleset内へ置いてください。新しいmodeを追加するときにosu!standardの判定を共通基盤へ混ぜないでください。
- Debug Autoは通常の`GameplaySession` Input APIだけを通し、Score、judgement、HitObject状態、Slider event、Spinner progressを直接変更してはいけません。
- osu!standardの挙動を再現・修正するときは、推測だけで実装せず、必要に応じて現在のosu!lazer実装を参照してください。
- 小さく、テスト可能な変更を優先してください。巨大な抽象化や不要なDI frameworkを導入しないでください。
- 要求された範囲を越える大規模なリファクタリングを勝手に行わないでください。

## テストと検証

- 変更した機能に対応するテストが存在する場合は必ず実行してください。
- バグ修正では、可能な限り再発を防ぐ回帰テストを追加してください。
- 原則として作業完了前に関連テストとGradle buildを実行してください。
- 既存テストの期待値が誤っていることを確認できた場合は、間違った挙動を維持するために実装を歪めず、根拠を確認した上でテスト側を修正してください。
- テストやbuildが失敗した場合、自分の変更が原因なら修正してから作業を完了してください。
- 自分の変更と無関係な既存の失敗がある場合は、その事実を最終報告に明記してください。

## Git / Commit

**ファイルを変更した作業は、コミットまで完了して初めて作業完了です。**

- コード、テスト、設定、ドキュメントなどを変更した場合は、作業終了前に必ずGit commitを作成してください。
- 自分が行った変更を未コミットのまま残して作業を終了してはいけません。
- 「変更しました」「buildが通りました」だけで終了せず、必ずcommitまで実行してください。
- commit前に`git diff`または同等の方法で変更内容を確認してください。
- commit後に、自分が変更したファイルが未コミット状態で残っていないことを確認してください。
- commitは論理的な単位で分けてください。大量の無関係な変更を1commitにまとめないでください。
- 機能追加、バグ修正、大規模リファクタリングは可能な限り別commitにしてください。
- commit messageは変更内容が分かる簡潔な英語を基本としてください。
- ユーザーが作業開始前から行っていた変更や、今回のタスクと無関係な変更を勝手にcommitへ含めないでください。
- 他人またはユーザーの未コミット変更を`reset`、`checkout`、`restore`などで消してはいけません。
- `git commit --amend`、rebase、force pushなど既存履歴を書き換える操作は、明示的に要求されない限り行わないでください。
- 変更が一切発生しなかった調査タスクでは、空commitを作る必要はありません。その場合は「変更なし・commitなし」と明記してください。

## 作業終了条件

変更を伴うタスクでは、以下を満たすまで完了扱いにしないでください。

1. 要求された変更が実装されている。
2. 必要なテストが追加または更新されている。
3. 関連テストを実行している。
4. 原則としてGradle buildが成功している。
5. 自分の変更内容を確認している。
6. 自分の変更をGit commitしている。
7. 最終報告にcommit SHAとcommit messageを記載している。

**特に6番を省略してはいけません。変更を行ったのにcommitせず終了することは禁止です。**

## ファイル・Importの安全性

- 生成物、Libraryの譜面データ、不要な一時ファイルをcommitしないでください。
- archive entryは展開前に検証してください。特に絶対パス、`..`、Windows形式のパスを拒否し、Zip Slipを防いでください。
- 壊れた譜面やarchiveはImportエラーとして扱い、アプリ全体を終了させないでください。

## 最終報告

変更を行った場合、最終回答では少なくとも以下を簡潔に報告してください。

- 何を変更したか
- 原因や設計上の判断
- 実行したテスト / buildとその結果
- 作成したcommitのSHA
- commit message

未コミットの変更を残した状態で「完了」と報告してはいけません。
